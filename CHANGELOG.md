# 变更日志

所有对纪律手册和系统的改动都记在这里。维护规则见 `AGENTS.md` §6。

版本号约定:小修 `+0.0.1` · 规则调整 `+0.1.0` · 策略姿态级改动 `+1.0.0`。

---

## v5.13.0(2026-08-18)· 怎么判断一条策略是好是坏

原来的审计只回答一个问题:**better 组的均笔盈亏是不是比 worse 组高。**
这个判据太弱,弱到会给出自信的错误结论。这一版补上三层,让它能回答那个真问题 ——
**"今天判定为坏,七天后会不会又变好?"**

### 新增 ②:环境可能变了(`regime_shift`)

一条只在下跌趋势里成立的规则,在市场转多之后必然连续"失效"。
只有「支持 / 不支持」两个出口时,它会被判成**规则错了** ——
然后你在市场即将回到它擅长的环境时,亲手把它退役。

现在审计把历史切成前后两段(**按标签各自切半再合并**,避免把"标签构成变了"
误判成"环境变了"),两段结论相反就给出第三种判定:

```
🔀 [环境可能变了]  前半段差 +$12.40,后半段差 -$8.10(近期反转为不支持)
   → 先别改状态。补一个适用环境的条件,分环境重新计数;
     确实要停用就降为 provisional 观察,不要直接 refuted。
```

**判"规则错了"的门槛因此高于判"没用":** 必须前后两段**都**不支持才建议 `refuted`,
一次反转只会让它回到观察期,不会被判死。

> 这一步**必须排在噪音检测之前**:一次完整的反转(前段 +12、后段 −12)在全期均值里
> 恰好互相抵消,信噪比接近 0 —— 先跑噪音检测会把它判成"数据不足",
> 而你永远看不到那个最重要的事实:**这条规则的有效性掉过头。**

### 新增 ③:差异是不是噪音

`$40 vs $38` 与 `$40 vs $5` 只看均值时结论一样,但如果组内波动都有 ±$30,
前者几乎肯定只是抖动。现在计算信噪比(组间差 ÷ 组内波动,近似 t 统计量):
< 1.0 判"数据不足",< 2.0 只给"弱支持/弱反驳"。
**样本量回答"够不够多",信噪比回答"差异是不是真的" —— 两个问题,缺一不可。**

为此 `stats.summarize` 的 `by_tag` 增加了逐事件明细(`values` / `dates`);
只有均值的话,"差异是真的"和"差异在噪音里"长得一模一样。

### 新增 ④:单次判定不算数

审计结果追加进 `data/audit-log.jsonl`(用户层)。`make stats` 会显示历次判定序列;
`--set-status` 在写入前把稳定性摆出来:

```
⚠️  最近 2 次审计在翻转:支持 → 不支持 —— 翻转期不要改状态
```

**一个会来回翻转的判定,本身就说明它不该被用来改状态。**
没有历史也算不稳定 —— 第一次看到某个结论,不该立刻据此改变一条规则能调动多少真钱。

### 现在的四道关卡

| | 问题 | 判据 |
|---|---|---|
| ① | 样本够不够多 | ≥ 10 个决策事件谈方向,≥ 20 谈改状态 |
| ② | 前后两段一致吗 | 相反 → `regime_shift`,**禁止改状态** |
| ③ | 差异是不是噪音 | 信噪比 ≥ 1.0 谈方向,≥ 2.0 谈改状态 |
| ④ | 结论稳不稳 | 最近 2 次审计同向 |

四关全过,审计也只产出**建议** —— 改状态仍然需要你 `--approved`。

README 新增同名章节;`modes/review.md` 的判定表从五种扩到六种;
`config/rules.example.toml` 写明四道关卡。测试 366 → 378。

---

## v5.12.2(2026-08-18)· README 瘦身 24%,重点前置

内容没删,**说法压紧了** —— 621 → 473 行。

- **「它到底给你什么」** 从七段小作文压成一张表(你得到的 / 靠什么实现),
  只保留两处展开:闸门挡住的具体错误(那张表本身就是最好的说服),
  以及**这个项目和一般交易机器人最不同的一点** ——
  「一般的交易机器人执行策略,但从不告诉你策略对不对」。
  规则闭环图之前讲的是机制,现在先讲它带来什么:
  **哪条纪律其实一直在帮你,哪条只是让你心安、实际在拖后腿。**
- **「记忆」** 从五个小节压成一张表 + 一段分层 + 一句"为什么是 3 和 14"。
- **三处边界合并成一节「边界与限制」**:不提供策略 / 不碰期权杠杆 /
  上限的买卖口径 / 当前真实缺口。原来它们散在三个地方、互相重复。
- **「核心特性」去重**:与收益表重复的行删掉,只留补充信息(审计的样本门槛、
  决策事件的定义、接入状态机是闸门、平台机制知识库等)。
- 「它是怎么自动的」「我为什么做这个」同样压缩,保留全部事实与那两次泄漏的自述。

顺序也调整成:是什么 → 给你什么 → 怎么记历史 → 为什么做 → 怎么自动 →
平台 → 补充特性 → 边界 → 快速开始 → 参考。

---

## v5.12.1(2026-08-18)· Windows CI 第一次运行就证明了 Windows 跑不起来

加上 Windows runner 的**第一次** CI 就红了 —— 这正是加它的意义:
在此之前 README 里的"Windows ✅"只是代码审查的结论。

两个真实缺陷(ubuntu / macOS 全绿,只有 Windows 失败):

**1. `✅` 让每一条命令都崩。** Windows 控制台默认编码是 cp1252 / cp936,
编不出 ✅ ❌ ⚠️,而本项目**每一行输出**都带这些符号 —— `UnicodeEncodeError`。
不是测试的问题,是**整套 CLI 在 Windows 上跑不完一条命令**。
现在 `meigu_lib` 导入时把 stdout/stderr 切到 UTF-8(输出被重定向到管道/文件时
同样需要:那时 Python 用的是本地编码)。

**2. 断言写死了 `/` 分隔符。** `assertIn("examples/", source)` 在 Windows 上
永远为假 —— 改成比对 `Path(...).parts`。

测试 364 → 366(新增两条:导入后 stdout 必须是 UTF-8;辅助函数遇到没有
`reconfigure` 的流不得抛异常)。

---

## v5.12.0(2026-08-18)· 能被调用方替换的上限不是上限

### P0

**1. `--profile` 是下单路径上的一个后门。**
评审实测:传入临时 profile 把单笔上限改成 $100,000,一笔本该 DENY 的 $500 买单
就变成了 `ALLOW`。`--tags` 能替换理由词表,`--now-et` 能绕过报价新鲜度、
意图时效与交易时段 —— **这三个都是风控输入,不是显示选项。**

现在:`--profile` **删除**;`--tags` / `--now-et` 只能配合 `--demo` 使用;
`--demo` 读死路径 `demo/profile-demo.toml`,并强制校验 dry_run + 占位账户,
且**结果永远不会是 `ALLOW`**(演示不该输出一句"可以下单"的截图)。

**2. 系统买不了从没持有过的股票。**
结构层要求 `position.market_value` / `qty` > 0,而提示又让未持仓时填 0 ——
不给 position 是"风控数据不完整",给 0 是"订单结构非法",**两条路都堵死**,
于是系统只能加仓、不能开新仓。
现在持仓字段允许 0;"卖出必须真的有货"改由「持仓股数」闸门单独把关。

### P1

**3. `today_orders` 子项可以是半条记录。** `[{}]`、`[{"amount": 50}]` 都能过 ——
缺 `symbol` 就漏掉同标的当日重复,缺 `side` 就漏掉当日累计。
**半条记录比没有记录更危险:它让闸门以为自己算过了。** 现在三个字段全部必填。

**4. 演练证据只证明"跑过",不证明"跑通"。**
一份结构损坏的日志照样写下 journal 证据,`record_drill` 只看 stage 名在不在,
于是照样记成"演练通过"。现在每条证据带 `ok` 字段;任一机器环节 `ok=false`
直接拒绝记录。

**5. 演练日志有并发覆盖窗口。** 原来是"先追加一行、再读全文改最后一行、整体写回",
两个脚本同时跑会改错行或覆盖对方刚写的记录。现在一次构造完整记录、单次追加 + fsync。

**6. "五节点自愈"名不副实。** 9:12 能重建另外四个,但**9:12 自己掉了就没人能重建它**;
五个同时过期同理。README / SKILL / premarket 全部改口为
**"锚点存活时的 best-effort 修复"**,并列表写清哪些能修、哪些不能。

### P2

- **CI 加上 Windows 与 macOS runner**(3 平台 × 2 个 Python 版本)。
  README 声称三平台可用,那就该由 CI 证明,而不是靠代码审查下结论。
  Windows job 装 `tzdata`,并新增一步"时区数据库可用"的显式验证。
- **测试文件里的 `unittest.main()` 全部移到文件末尾。** `test_rules.py` /
  `test_dashboard.py` / `test_stats.py` 都有中段 main:`python tests/test_rules.py`
  只跑 24 个用例就退出(discover 是 51 个)—— **本地直接跑给出虚假的全绿**。
  并加了一条测试:任何测试文件都不许在 main guard 之后再定义 class。
- gif 在 VERSION bump **之后**重录,并在录制脚本里写明这个顺序要求。

### README

- **快速开始改成对齐的六步**(第一步…第六步),每步都说清做什么、为什么。
- **新增「它怎么记住历史(以及为什么不会越堆越长)」** —— 回答三个问题:
  记在哪(日志 / 规则证据 / 复盘档案,三种时间尺度)、怎么压(3 个交易日全细节 /
  14 个交易日单行 / 更久按周合并 / 150 行硬上限)、**为什么是这两个数字**
  (决策窗口 vs 归档窗口需要的粒度不同;早期用过单一"7 天"阈值,
  问题是它把两件事混成了一件)。并说明**没有任何历史会被整体拼进提示词**。
- 收紧几处绝对表述:"你在睡觉也照样发生"→ 加上"只要会话开着、机器醒着";
  "数据永远不会泄露"→ 改为说明防线有几层,并**如实写出自己泄过的两次**
  (测试固件里的真实账户号、录 gif 时读了真实配置)——
  **没有"永远不会",只有"每一层都有测试盯着"。**

测试 359 → 364。

---

## v5.11.0(2026-08-18)· 先说它给你什么,再说它不给你什么

### README 的开篇换了顺序

原来第一屏是「⚠️ 先说清楚这个项目不提供什么」。诚实,但顺序错了 ——
**读者还不知道这东西是干什么的,先读到一串"不提供"**。

现在开篇直接说:**这是一个 AI 自动炒股的辅助系统**,默认在每个交易日设置
**五个 cron 节点**,到点后 AI 自己醒来、自己分析、自己决定买卖。
紧接着是「它到底给你什么」——七条,每条都说清**靠什么机制实现**:

1. 盯盘不再消耗你(五个时点自动触发,而且程序不会疲劳)
2. 每一笔单都过确定性闸门(附表:哪道闸门挡住哪种事后才发现的错误)
3. 你的纪律第一次有了"对错"的判据(样本不足时审计器拒绝下结论)
4. 仓位随证据自动缩放,不用人工换算
5. 遗忘的成本消失(尾盘日志 + 分层压缩)
6. 策略与账户数据永远不进仓(硬隔离 + 扫描器 + CI)
7. 全部可审计、零依赖

「不提供交易策略」那段保留,但移到自我介绍之后 —— 它是**边界**,不是开场白。
英文 README 同步。

### 演示 gif 大幅简化

原来四个场景、124 秒:状态机 → 九项验收 → 仪表盘三页(含 FIFO 配对与规则审计)
→ 闸门拦单。**信息量大,但对第一次看的人是负担。**

现在两个场景、26 秒,只回答新用户真正会问的两个问题:

1. **clone 之后我该干什么?** → `make setup` 告诉你在第几步
2. **它凭什么值得托付真钱?** → 闸门真的拦下一笔(意图部分减仓,实际会卖掉 90.9%)

仪表盘、FIFO 配对、规则审计**刻意不再演示** —— 那些是用起来之后才关心的。
**演示的目的是让人看懂,不是让人看全。**

体积 989K → 378K。

顺带新增 `make setup-demo`(等价于 `setup.py --demo`),这样录制脚本里
不需要任何"看起来像 make setup 但其实不是"的伪装 —— 演示命令就是真命令。

测试 358 → 359(录制脚本的约束测试改为按新叙事断言:必须展示 onboarding 与闸门;
按键解析的用例改为直接测解析器,不再依赖录制脚本里恰好有没有按键)。

---

## v5.10.0(2026-08-18)· 说清"它到底怎么自动",并按真实流程重录演示

### README 补上四件一直没说清的事

**1. 它是怎么"自动"的。** AI 在五个时刻自己醒来、自己分析、自己决定买卖,
载体是 Claude Code 的会话级定时任务。

**为什么只有五个节点 —— 这是 token 预算问题,不是能力问题。**
每个检查点都是一次完整分析(行情 + 持仓 + 新闻 + 规则比对 + 结论)。
按"每 15 分钟看一眼"来排,**前几天就能把一个月的额度烧光**,
然后剩下三周一个检查点都跑不了。五个节点覆盖一天里信息密度最高的时点,
而不是把算力平摊在噪音上。初始化之后可以自建节点(财报日、FOMC、CPI),
但加之前先想清楚它花的 token 值不值那次判断。

**自愈**:9:12 的 `premarket` 每天核对另外四个节点是否还活着,掉了就重建。
生效前提三条,缺一不可:会话窗口开着、电脑接电、电脑不休眠。

**2. 平台支持(逐项核过,不是猜的)。**

| | macOS | Windows | Linux |
|---|---|---|---|
| 脚本与闸门 | ✅ | ✅ | ✅ |
| 仪表盘 TUI | ✅ | ⚠️ 需 WSL 或 `windows-curses` | ✅ |
| 防休眠自检 | ✅ | ➖ 自动跳过 | ➖ 自动跳过 |
| 时区数据库 | 自带 | **需 `pip install tzdata`** | 自带 |

**权限上没有特殊要求**:不需要管理员权限、不写系统目录、不装后台服务。

顺带修了一个真实的跨平台缺陷:Windows 没有系统时区数据库,
`ZoneInfo("America/New_York")` 会直接抛 `ZoneInfoNotFoundError` ——
而本项目**所有**时间判断都基于 ET。现在这个异常被翻译成一句能照做的话
(`pip install tzdata`),`doctor` 也加了对应自检。

**3. 第一步是你自己动手连 Robinhood MCP。** 这一步 AI 替你做不了,也不该替你做 ——
它需要你的券商账号登录。README 里写清了:手动连接登录,然后 AI 做只读验证。

**4. 它不碰什么。** 期权、杠杆/融资、裸空一概不做,只做正股买卖。
所有闸门默认取值偏保守,**默认不做冒险动作**。当然你可以配一套激进的规则 ——
系统不阻止你冒险,只保证你冒的险是被记录、被检验、能被数据推翻的。

### 演示 gif 按新用户流程重录

叙事顺序改成 clone 之后的真实顺序:接入状态机 → 九项验收清单 →
仪表盘三页 → 下单闸门真的拦住一笔。

新增 `setup.py --demo`:用 `examples/` 固件跑**同一套判定代码**,
不读真实配置。停在"演练未做"这一步是刻意的 ——
新用户最该看见的不是全绿,而是"真钱执行还没开,因为演练还没跑"。

### 录制时发现并堵住的隐私缺口

第一版重录后检查帧,发现 gif 里出现了 `配置 ***4351` ——
**preflight 读了真实的 `config/profile.toml`,把真实账户号的后 4 位录进了公开 gif。**
gif 是二进制,进仓后既难审计也难撤回。

修法:`preflight.py` 新增 `--profile`,演示用 `demo/profile-demo.toml`(全占位);
并加测试强制录制脚本里的 preflight 必须带 `--profile demo/` ——
**又一次"防御必须有测试证明它生效"**。

测试 357 → 358。

---

## v5.9.0(2026-08-18)· 结构先行:崩溃不等于拒绝

第三轮对抗测试找出 5 类仍可绕过的路径。它们有一个共同形状:
**闸门本身是对的,但它假设字段"是个能算的数"** —— 假设不成立时,
闸门要么被跳过,要么直接崩掉。而在自动化链路里,
**崩溃不等于拒绝**:调用方看到的是 traceback,很容易被当成偶发故障重试一次。

### P0

**1. 两套尺寸口径可以互相掩护。**
`{"amount_usd": 1, "qty": 100, "price": 100}` 会按 $1 过闸门、按 $10,000 成交 ——
所有金额上限一次全废。旧实现无条件优先 `amount_usd`。
现在:两者同时出现时必须自洽(容差 5%),否则结构层就 DENY;
`_order_amount` 也改取两者较大值,即使校验被绕过也倾向更严。

**2. 缺字段成了免检通道。**
只给 `buying_power` 不给 `total_value` / `equity_value` / `position`,
集中度与现金底线两道闸门自动跳过;完全不给 `today_orders`,
当日累计、笔数、同标的重复三道一起清零。
现在:买单缺任一风控字段 → 「风控数据完整性」DENY;`today_orders` 缺失 → DENY
(没有就显式给 `[]`);其中的负数金额 → DENY。

**3. 字符串 `"false"` 被当成真。**
`enabled = "false"` 让总开关常开,MCP 九项验收提交 `"false"` 也照样记"通过" ——
Python 里非空字符串为真。现在全部改为 `is True` / `is False` 严格判定,
并在配置层直接要求原生布尔值。

**4. 演练 run id 的锚点在被验证方手里。**
不跑 `--start-drill`、自造一个 id,preflight 照样写证据,`--record-drill` 照样接受。
现在:没有进行中的 run 一律拒绝;preflight 只为**当前 active run** 写证据
(run id 从 state 读,不采信订单里的值);证据绑定 nonce 与账户指纹;
记录成功后该 run 立即作废。

另外,原来的"证据"只证明 preflight 跑过。现在要求三个环节各自留下机器证据 ——
`preflight`(订单带 run id)、`journal`(`make journal-check`)、`review`(`make stats`);
`premarket` / `check` 没有对应脚本,仍是自报,**记录里如实标注**,
不再把两者混成一句"演练通过"。

**5. 卖单的持仓证据可以省略。**
只给 `market_value` 不给 `qty`,卖 100 股照过。现在按股数下卖单必须提供
`position.qty`;`intent=close` 还要求拟卖数量与持仓吻合(≥99.5%)——
否则等于用清仓通道换取笔数豁免。

### P1

- **非法输入一律结构化 DENY,不再 traceback。** 新增订单/配置 schema 校验:
  类型、有限数、正数、枚举(`order_type` / `market_hours` / `intent`)。
  结构不合法就地 DENY,不再往下算数值闸门。
- `kill_switch_file` 指向仓外时,原来抛 `ValueError`;现在判为"急停开关形同虚设"→ DENY。
- **倍率序关系校验:** `observe <= weak <= supported`,否则可以配出"越没证据仓位越大"。
- **`doctor` 接入状态机结论。** 原来 setup 三步未完成,doctor 仍报"全部必检项通过" ——
  两个入口对同一台机器给出不同结论时,人会记住更好听的那个。
  现在 dry-run 阶段提醒、真钱配置下必检失败。
- **"先 MCP、后 profile"不再自相矛盾。** 没有 profile 时完成只读验证,
  原来会显示"无从绑定"并退回第 2 步。现在这是正常中间态:
  显示"已验证 · 待绑定",下一步指向 profile。

### 测试与文档

- **删掉重复插入的测试块。** `tests/test_preflight.py` 里 9 个类各定义了两次,
  Python 只保留后一份 —— 前一份从未被执行。删除后测试数不变(339),
  正说明那份重复从来没算进覆盖。
- 修 `tests/test_stats.py` 把 `vocab` 误传给 `Path()`(3.14 会移除该行为)。
- `config/profile.example.toml` / `modes/setup.md` / `setup.py` 里的
  "四条无条件生效"改成买卖分列的准确表述,与 README 一致。
- 明确区分**订单字段** `primary_rule_id` / `context_rule_ids` 与
  **台账字段** `rule_ids`(第 11 列)—— 两者同名不同物。

测试 339 → 357。

### 仍未关闭的一点(架构性)

评审建议"由下单网关自己调用 Robinhood MCP 获取持仓/资产/当日订单,
覆盖订单里的自报字段"。**这一条本版没有做,因为 Python 脚本调不到 MCP 工具** ——
MCP 只有 agent 能调,`preflight` 是个纯脚本。当前能做的是把所有风控字段变成
**必填且 fail-closed**(本版已做),但"事实由谁提供"这个信任边界仍然存在:
提供事实的和接受检查的是同一个 agent。真正关闭它需要一个能自己调 MCP 的
常驻下单网关进程,那是独立的一块工作。

---

## v5.8.0(2026-08-18)· "我不想自己定策略"也是一条正式路径

原来第 4 步只有一条路:你自己回答那些问题。铁律第 8 条甚至明确禁止 AI 提规则。
但确实有人不想自己定策略,而"绕过限制让 AI 随便写"是最坏的解法 ——
那样既没有免责说明,也留不下"这是谁写的"。

### 两条路线,由用户选,不默认

```
1. 我自己写 —— 陪我回答那些问题,把我的判断变成规则
2. 我不想自己定 —— 你按当时的分析来定,我确认
```

选 2 时顺序不能变:**先说免责 → 逐条念规则原文 → 用户确认 → 才写入**。

免责要点(AI 必须原话说清):代拟的策略同样可能是错的、它不掌握你的风险承受能力
与资金用途、最终决策与结果由你负责。

### 写入必须带出处

`rules.py --add-rule <id> "<命题>" --origin ai --approved`

- 不加 `--approved` 直接拒绝写入。
- 写入自动记 `origin = "ai"` 与 `approved_at`(用户确认的时间)。
- 校验层同样要求:`origin = "ai"` 却没有 `approved_at` 的规则**加载即失败** ——
  手写进文件也绕不过。

**没有这两个字段就分不清"用户选择了信任 AI"和"AI 自己往文件里写了一条"。**

### 代拟的规则不享受任何特权

| | 你写的 | AI 代拟、你确认的 |
|---|---|---|
| 起始状态 | `hypothesis` | `hypothesis` |
| 起始仓位档 | 最低档 | 最低档 |
| 升级 | 数据支持 + 你批准 | 数据支持 + 你批准 |
| 被推翻 | `refuted` | `refuted` |

**你可以把"定策略"交给 AI,但不能把"检验策略"交出去** —— 检验只能由你的真实台账
完成,这恰恰是整个项目存在的理由。复盘时还能按 `origin` 分开看:
我自己写的和 AI 代拟的,哪一类被数据推翻得更多?

### 其他

- 铁律第 8 条相应改写:不是"永不提规则",而是"未经用户选择并确认就写入规则,
  任何情况下都是违规"。
- README 补了一段作者自己账户的经历(收益波动收窄、目前稳定在 5% 左右),
  并在紧邻处写明那是个人经历、不是系统承诺,也不构成收益预期。
- 测试 334 → 339。

---

## v5.7.0(2026-08-18)· 把状态机接到下单路径上

上一版做出了接入状态机,但它和实盘执行是**两条平行链路**:状态机只能告诉你
"你跳过了只读验证和演练",拦不住下一笔真单。对抗测试构造了 setup 未完成、
订单本身合法的场景,`preflight` 返回 `ALLOW`。

**提示不是闸门。** 这一版把两条链路接上,并补掉一批 fail-open。

### P0

**1. 接入状态成为下单闸门。** 每一笔真单进 `preflight` 都要重新验六步状态,
任一缺失即 `DENY`;评估失败也 `DENY`(fail-closed)。
不在 `--authorize-live` 时检查一次就完事 —— `[execution]` 是个可以手改的 TOML,
旧配置、手改、复制别人的配置都能绕过那一次检查。
dry_run 不受此限:演练本身是第 5 步,要求它先完成第 5 步是死循环。

**2. 只读验证绑定账户指纹。** 原来只存后 4 位,换账户后状态不会失效 ——
与文档承诺的"换账户自动回退"不符,而且后 4 位相同的两个账户会被认成同一个。
现在存 `SHA256(本机盐 + 规范化账户号)`:不可反推,账户一变,
只读验证与演练记录**自动失效**。

**3. 演练不再接受自报。** 原来 `--record-drill` 只收六个布尔值 ——
等于让被检查方出具检查结论;在 `enabled=true, dry_run=false` 下也能记成"演练完成"。
现在:`--start-drill` 生成 run id → `preflight` 把每次判定写进
`data/drill-runs.jsonl` → `--record-drill` 要求存在判定为 `DRY_RUN` 的证据行,
且前三步就绪、当前确实在 dry_run。

**4. 本机改回 dry_run。** 只读验证与演练都没做过,真钱开关却是开着的 ——
正是这套设计要防的情况。已改 `dry_run = true`,补齐六步后再用
`--authorize-live` 恢复。

### P1

- **非法 `live_mode` 从 fail-open 改为 fail-closed。** 原来只判断"是不是 guarded",
  于是 `live_mode = "invalid"` 落进宽松分支拿到 ×1.0 满尺寸。现在两道:
  「配置合法性」闸门直接 `DENY`;尺寸计算改判"是不是 autonomous",未知值按最严。
- **`--authorize-live` 回读校验。** 原来用正则替换,配置里若没有 `enabled` 这一行
  就命中零次、却照样报告成功 —— **提示成功、实际没开**。现在区分"改"和"插",
  写前解析校验、原子落盘、写后回读再校验,任一步不符就不报成功。
- **状态文件原子写。** 半截 JSON 会让状态静默退回空状态。
- **配置取值校验。** 负数上限、非数字、`max_orders_per_day = 0`、
  尺寸系数 > 1、指向仓外的 kill switch —— 存在不等于合法,一个负数上限等于没有上限。

### 之前几轮遗留的 fail-open,一并修掉

| 问题 | 现在 |
|---|---|
| 同标的当日交易过后,紧急清仓被"重复"拦截 | `intent=close` 豁免 |
| 达到当日笔数上限后,紧急清仓被拦截 | 同上豁免 |
| 持 1 股卖 2 股在某些金额下仍 ALLOW | 新增「持仓股数」闸门,股数单独比 |
| 缺 `account_id` 只是警告 | 改为 `DENY` —— "无法核实"不等于"核实通过" |
| 买单缺 `buying_power` 时跳过买力闸门 | 缺数据即 `DENY` |
| 台账损坏时 `LedgerError` 被吞,订单照过 | 新增「台账可读」闸门,去重失效即 `DENY` |
| 文档要求写 `rule_ids`,代码只认 `primary_rule_id` | 统一契约;旧字段直接 `DENY`,不再静默降到 40% |

**紧急清仓通道的代价:** 豁免只属于 `side=sell` + `intent=close`,
且必须提供 `position` 数据(「清仓证据」闸门),账户身份与持仓股数照常约束。
**风控如果能阻止你降低风险,它就不是风控** —— 但豁免必须有代价,否则就是后门。

### 文档

- README 的"四条硬上限无条件生效"是不准确的省略 —— 换成买入/卖出分列的表格,
  写清每条豁免的条件。
- 修掉中文段落里那些会**渲染成多余空格**的换行(Markdown 把换行渲染为空格,
  夹在两个汉字之间就是一个可见的空格),并把"连续第 20 天"改成"连续多天"。

### 测试

295 → 334。新增全部对抗场景的回归用例:状态机闸门(含 dry-run 例外与 fail-closed)、
账户指纹失效、演练自报被拒、非法 `live_mode`、缺字段插入、紧急清仓的豁免与边界、
超持仓股数、缺 BP、台账损坏、旧字段 `rule_ids`。

闸门测试还补了一个隔离:`preflight` 的接入状态查询被打桩 ——
否则"本机做没做过只读验证"会决定测试是绿是红,那是已经修过一次的病。

---

## v5.6.0(2026-08-18)· 首次接入状态机:连接要早、只读;授权要晚、单独

上一版把"能连上券商"和"能下真单"绑在了同一个动作里。这一版把它们拆开。

### 为什么要拆

这个项目的核心是"AI + 券商 MCP 自动交易"。若把连接券商放到最后,用户会配置半天
才发现账户、权限或 MCP 根本不可用 —— **而那时 MCP 的问题和项目配置的问题已经混在一起**,
分不清是谁的错。

但连接成功**不等于**获得真钱执行权限。所以正确的顺序是:

    先只读连上 → 再配账户与上限 → 再写自己的策略 → 再演练 → 最后单独授权

### `make setup` —— 六态状态机

| 状态 | 通过条件 |
|---|---|
| `UNINITIALIZED` | Python 版本、仓库完整性 |
| `MCP_CONNECTED_READONLY` | 券商**只读**验证:读账户 / 定位子账户且唯一 / 持仓 / 买力 / **带时间戳**的报价 / `review` 可用 / 全程**未**调用 `place` / 异常时 fail-closed |
| `PROFILE_READY` | 账户号非占位 + 四条硬上限齐全 |
| `STRATEGY_READY` | 词表 + 规则 + 散文策略齐全,且**至少一条市场判断类规则** |
| `AUTOMATION_READY` | dry-run 端到端演练跑通(盘前→盘中→preflight→模拟下单→日志→复盘) |
| `LIVE_AUTHORIZED` | 前五步全过 + 用户明确批准 |

**状态是算出来的,不是记下来的**:改了账户号、删了规则、规则文件读不出来,
状态会自动回落。`data/setup-state.json` 只记录两件无法从文件推断的事
(只读验证与演练的结果),属于用户层。

### 脚本与 agent 的分工

MCP 工具只有 agent 能调。所以**脚本拥有状态判定与裁决,agent 执行调用并写回结果**:

```bash
setup.py --record-mcp --stdin      # 9 项逐条校验,缺一不可
setup.py --record-drill --stdin    # 6 个环节逐条校验
setup.py --authorize-live guarded --approved
```

写回会被逐项校验:缺项、读到的账户号与配置不符、或**验证时执行开关是开着的**,
都会被拒绝 —— "验证通过"不能只是 agent 的一句自我声明。

### 真钱执行分两档

`execution.live_mode`:

- `guarded` —— 仓位统一压到最低档,**不管规则状态多好**
- `autonomous` —— 仓位按规则 `status` 缩放

**开真钱与放开仓位是两个独立决定。** 先做前一个,跑一段确认健康,再做后一个。

### Demo 分支

`make setup` 会先问走哪条路。只想看效果的人不该被迫授权券商 ——
`make dashboard-demo` 与 `stats.py --demo` 用虚构固件跑完整工作流,不需要任何账户。

### 顺带发现的事

状态机第一次运行就指出:**本机此前已开启真钱执行,但券商只读验证与 dry-run 演练
都没做过** —— 那正是这套设计要防的情况。

### 两个扫描器曾对同一行给出相反结论

本地 `check_privacy.py` 认 `# privacy-allow` 逐行豁免,CI 的 git 历史扫描不认。
结果:本地全绿,推上去 CI 红 —— 而且历史一旦进去,只能重写历史才能变绿。

**豁免一行的真实代价不是"跳过一次检查",而是"这一行永久卡住 CI"。**

修法不是放宽 CI(那等于关掉警报),而是加一条不变量测试:
被 pragma 豁免的 9 位数字**必须同时是 CI 认可的占位形态**。
该测试从 workflow 文件里**解析**占位正则,不复制一份 —— 复制就是第二个真相来源。

### 其他

- 修 `setup.py` 内部路径耦合:状态判定统一直读 `CONFIG_DIR/profile.toml`,
  不走会回退到 `.example` 的 `load_config()`。
- 测试 274 → 295。

---

## v5.5.0(2026-08-18)· 修掉真钱模式的七个阻塞问题

外部测试在 v5.4.0 上发现 8 个问题,其中 7 个是真实缺陷。逐条修复:

### 🔴 退出敞口被风控挡住(最危险的一条)

实测:清仓 $120 被 `max_order_usd = 80` 与证据尺寸同时拒绝 ——
**系统允许你建仓,却不允许你完整平仓。** 这不是保守,是会造成真实损失的 bug。

修复原则:**风控限制的是"能承担多少风险",不是"能不能降低风险"。**
卖出现在豁免于:证据尺寸缩放、单笔金额上限、单日金额上限。
改由持仓市值本身封顶(不能卖出超过你持有的),其余闸门(减仓占比、时间戳、
同标的当日重复等)照常生效。

### 🔴 规则/台账加载失败时仍然放行

异常分支是 `fatal=False` → 读文件出错也会 ALLOW。真钱模式必须 **fail-closed**:
算不出证据等级就不许下单。

### 🔴 流程规则可以套到 100% 尺寸

`kind="process"` 直接返回 1.0,于是引用任意一条**公开的**流程纪律就能绕开 40% 限制。
现在只有 `kind="market"` 的规则能决定尺寸;引用流程规则作为主依据会被拒绝 ——
流程纪律是安全闸门,不是下单理由。

### 🔴 "取最强依据"可被利用

对多条依据取 `max()` → 引用一条弱规则再附带一条强规则就能拿强规则的尺寸。
改为**单一 `primary_rule_id` 决定尺寸**,`context_rule_ids` 仅作记录。

### 🔴 "状态需批准"与实际尺寸行为不一致

旧实现由**审计结果**决定尺寸:攒够 20 个样本就自动满额 —— 用户从未同意过,
"改状态需要批准"形同虚设。现在**尺寸只由 `status` 决定**,审计只产生升级建议。
新增 `provisional` 状态作为中间档(0.7),同样需要用户批准。

### 🔴 规则写入可能损坏文件

实测:含换行的证据会让 TOML 立刻损坏;`--kind` 非法时先报成功再读不出来;
`refuted → supported` 后 `execution_scope` 仍是 `none`,规则永久卡死。

现在写入是:临时文件 → 完整解析与交叉校验 → 原子替换;失败则原文件一字节不改。
换行/制表符/引号全部转义;`kind` 写前校验;状态与作用域双向同步。

### 🟠 evidence 与样本数没有连接(文档错误)

我此前说"每天记 evidence,几个月后自动升档"是**错的**:审计样本来自**台账**
(带相应标签的卖出笔数),不是 evidence 行数。已在 `docs/DATA_CONTRACT.md` 更正,
并新增可选的第 11 列 `rule_ids`,让"这笔依据哪条规则"可被追溯(旧 10 列台账仍可读)。

### 🟠 "改三个开关就是全自动"是过度承诺

仓库没有持久调度器,也无法从代码层面阻止 agent 绕过 preflight 直调 MCP。
README 新增「关于"全自动"的边界」一节,明确当前是**"有人在场的自动化"**:
开关只代表"这次运行里可以真下单",不代表无人值守。

### 其他

- README 恢复个人动机段(只讲它教会了什么,不含标的、日期与具体规则)。
- 测试 261 → 274。

---

## v5.4.0(2026-08-18)· 证据决定尺寸,而不是决定能否交易

### 🔴 上一版的闸门设计是反的(已修)

`规则作用域` 闸门:订单**不声明** `rule_ids` → 全额放行;**声明了**且依据仍在
观察期 → 拒绝。**这惩罚诚实、奖励沉默** —— agent 只要不说依据了哪条规则就能满额下单。
作为安全机制它失效,作为自动化障碍它却是真实的。

更根本的问题是它制造了**引导期死锁**:第一天每条规则都没有数据支持,
若"未验证"等于"不许交易",就永远攒不到验证它所需的数据。

### 改为按证据强度自动缩放仓位

| 依据强度 | 单笔上限 |
|---|---|
| 未声明 / 观察期(<10 个决策事件) | `max_order_usd × size_scale_observe`(默认 0.4) |
| 弱支持(10–19 个事件) | `× size_scale_weak`(默认 0.7) |
| 已支持(≥20 个事件,或用户已批准升级) | `× size_scale_supported`(默认 1.0) |
| `refuted` / `retired` | 拒绝 |

- **未声明 `rule_ids` 按最低档**,不再是全额放行 —— 让诚实声明变成有回报的事。
- 多条依据取**最强**的一条:引用额外的弱上下文不该受罚。
- 超限时 preflight **直接给出允许金额**,agent 按那个数重下 —— 全程无人工换算。
- `max_order_usd` / `max_daily_usd` / `max_orders_per_day` / kill switch 四条硬上限
  **不随证据变化**,任何规则都绕不过。

这样自动交易从第一天就成立,仓位随证据积累自动放大。

### 自我进化的写回环节机械化

新增 `scripts/rules.py` 写入命令,取代手编 TOML(手改会破坏格式或写错字段,
而这个文件一旦坏掉,整套审计会**静默失真**):

```bash
rules.py --record-evidence <id> "…"                  # 只记事实 → 免批准
rules.py --set-status <id> supported --approved      # 改变能否指导下单 → 需用户批准
rules.py --add-rule <id> "一句能被证伪的话"            # 新假设,默认 hypothesis → observe
```

- `--set-status` 不带 `--approved` 直接退出并提示先向用户说明。
- 降级为 `refuted` / `retired` 时自动把 `execution_scope` 设为 `none`,
  避免"状态说停用、作用域还在跑"。
- 写入采用**外科式文本编辑**:只改目标字段,保留全部注释与格式。
- 16 个新测试锁住:每次写入后文件仍可解析、不误伤相邻规则、未批准不得改状态。

### 其他

- 测试 244 → 261。

---

## v5.3.0(2026-08-18)· 策略与框架彻底分离

复审指出:底层已经通用化,但**上层 AI 工作流仍在执行原作者的个人策略** ——
`_shared.md` 里 10 个理由标签、现金目标、准入判定、催化窗口全都还在,
而 preflight 已经改读中立词表。**AI 生成的每一笔都会被自己的闸门拒绝。**
这一版把策略层整体移出仓库。

### 🔴 端到端断裂(已修)

公开 `modes/` 与 `templates/` 里有 6 个文件硬编码了 10 个理由标签,
而脚本读的是 `config/reason-tags.toml`。现在公开层**零个标签字面量**,
全部指向配置;新用户 clone 后必须先定义自己的词表才能记台账。

### 🔴 干净 clone 缺文件(已修)

`.gitignore` 的 `*portfolio*` 兜底通配把 dashboard 的演示固件一起吞了 ——
本地测试全绿,但干净 clone 跑不起来。固件改名 `sample-snapshot.json`,
并对 `examples/**` `demo/**` 显式反向放行。

### 策略层整体私有化

| 文件 | 内容 | 进仓 |
|---|---|---|
| `modes/_mechanics.md` | 平台机制与执行陷阱(去日期/标的/金额) | ✅ |
| `modes/_strategy.example.md` | 只有问题清单,零答案 | ✅ |
| `modes/_strategy.md` | 你的策略 | 🔒 |
| `config/reason-tags.toml` | 你的标签词表 | 🔒 |
| `config/rules.toml` | 你的可检验规则 | 🔒 |

**怎么细分交易理由、相信哪些规则,就是策略本身。** 仓库只给 `*.example.*` 模板。

### 自我进化闭环:`scripts/rules.py`

- 三类规则:`invariant`(不可关闭的流程不变量,无程序把守)/ `enforced`(有闸门把守)/
  `market`(由台账数据裁决)。混淆这三者会让"程序强制"变成一句空话。
- **执行层级与状态正交**:`live` / `observe` / `none`。`hypothesis` 默认只能 `observe`
  —— **零样本的新假设不该拿真钱去试**。preflight 新增「规则作用域」闸门强制这条。
- 审计结论拆成**方向**(supports/refutes/inconclusive)与**强度**
  (insufficient/weak/moderate/strong):<10 事件不下结论,10–19 只给弱支持/弱反驳,
  ≥20 才可**建议**改状态 —— 而任何状态变更都需要用户批准。
- **样本单位是独立决策事件**,不是 FIFO lot:一笔卖单匹配三个买入批次仍算一次决策。
  多标签比较按事件数加权。
- 交叉校验:规则引用的标签必须存在于词表、`enforced_by` 必须命中
  `preflight.GATE_NAMES` —— 写错闸门名会让规则显示「程序强制」但其实无人把守。
- `config/rules.toml` 是执行状态的**唯一真相源**;`_strategy.md` 只提供解释。
  `make rules-check` 检查两者同步。

### 日报骨架也 config 化

指数、板块、主题、技术面标的、分组、风险维度全部来自 `config/watchlist.toml`。
模板里不再出现任何具体行业、主题或个股名 —— 有测试守着这条。

定位随之明确:**通用美股纪律框架**,不是某个方向的研究体系。

### 其他

- `journal` 的活文档回写与新状态机对齐:每日只追加 `evidence`,
  不改状态、不删规则、不动 `CHANGELOG`/`VERSION`(策略变更不属于系统层版本日志)。
- v1.0–v4.12 的历史版本记录移入用户层 `data/history.md`。
- 演示固件彻底中性化(A/B 占位标签),演示里的假设也是 `observe`。
- 测试自带词表,不再依赖本机私有配置 —— 换任何人 clone 结果一致。
- 测试 240 → 244。

---

## v5.2.0(2026-08-18)· 只读仪表盘 TUI + 可复现的演示录制

### 新增 `scripts/dashboard.py` —— curses TUI,零依赖

三个标签页,按"每天真正要看什么"选的,不是照抄别的项目:

| 页 | 内容 |
|---|---|
| **组合** | 持仓占比条 / BP vs `bp_target_pct`(进度条)/ 集中度 vs `max_single_pct`(超限标 `!`) |
| **台账** | `data/trades.tsv` 每一笔,可按日期/标的/金额/标签排序;Enter 展开该笔的 **FIFO 配对**(逐个买入批次 + 持有天数 + 实现盈亏) |
| **纪律** | 按 `reason_tag` 的绩效表 + **核心纪律是否被数据支持**(支持 / 不支持 / 数据不足,只有这三种,不含糊) |

**★ 设计:整个 UI 是纯函数** `render_*(data, width, height, state) -> list[str]`,
curses 只负责贴字符。所以界面能被单元测试完整覆盖 —— 本项目反复吃过的教训是
"防线必须有测试",而**错位的表格会被人当成数据读错**,UI 也是防线。

核心不变量:**每行显示宽度精确等于 width、行数精确等于 height**。
这条测试当场抓出两个真实 bug:
- `pad()` 截断时若跳过的是双宽字符,`out + "…"` 会比 n 少 1 列 → 整张表错位。
- curses 循环画 `lines[:height-1]`,把最后一行的**快捷键栏整行丢掉了**。

仪表盘是**只读**的:不下单,不改任何文件。

### 新增可复现的演示录制

`demo/demo.tape`(vhs)→ `docs/demo.gif`,`make demo` 一条命令重录。
四个场景:组合页 → 台账页排序与 FIFO 详情 → 纪律体检 → **重放 2026-07-29 的真实事故**
(意图"部分减仓"但仓位已缩水,闸门算出会卖掉 90.9% → DENY)。

35 秒 / 1180×624 / **668 KB**(对照:career-ops 的 demo.gif 是 8.3 MB)。

**隐私:gif 是像素,进公开仓后无法被 `check_privacy.py` 审计** ——
所以约束前移到生成环节:`tests/test_demo_tape.py` 强制录制脚本只能跑 demo 模式与
`demo/` 固件,并断言演示固件**真的会触发闸门**(固件若失效,gif 就在说谎)。

同一类思路的第二次应用:一个无法事后检查的产物,必须在生产环节把输入限死。

### 数据契约新增 `normalized`

`data/snapshots/*.json` 的 `checkpoints[*].normalized` 现为仪表盘的**唯一**数据源
(`total_value` / `cash` / `buying_power` / `equity_value` / `positions[]`)。
券商原始字段名不稳定,**脚本不猜** —— 缺这一块时仪表盘明确提示,不静默显示 0。
规范见 `docs/DATA_CONTRACT.md` §4,示例 `examples/sample-portfolio.json`(全虚构)。

### 其他

- `doctor` 的电源检查:`ok` 此前恒为 `True`,导致电池模式显示 ✅ 却附带警告文案,
  且不计入提醒计数 —— 图标与结论自相矛盾,已修。
- `preflight.py --example` 曾被 argparse 的 `required=True` 互斥组拦下,
  `make preflight-example` 根本不能用。**`run()` 全绿不代表 CLI 可用**,已补 5 个 CLI 入口测试。
- CI 冒烟测试增加三个标签页的 `--render` 与 `preflight --example`。
- 测试 142 → 217。

---

## v5.1.0(2026-08-18)· 安全与真实性修正

首版推送后的复审(外部 + 自查)发现的问题。**其中一项是已发生的真实泄漏。**

### 🔴 真实账户号曾泄漏到公开仓(已处置)

`tests/test_privacy.py` 用**真实账户号**当测试固件并被推送到公开仓库,暴露约 70 分钟。
三道本应拦住它的防线被同时关掉:

1. 拿真实账户号当固件(应该用虚构号)
2. 该文件被加进**整文件白名单** `ALLOWLIST_FILES`,扫描器跳过了它
3. CI 的账户号扫描**显式排除了这个文件**

排查中又发现第四重失效:那道 CI 扫描**从一开始就是空转的** ——
`git grep -E` 不支持 `\b` 词边界,正则从未匹配过任何东西,却一直报绿。

**处置:** 固件改虚构号 → 整文件白名单改为**逐行 `privacy-allow` pragma** →
CI 改为「模式选择即自检」(注入已知坏值证明能匹配,匹配不到就 fail)+ 遍历全部 ref →
改用 `printf` 避免 `echo` 展开 `\n` 导致过滤失效 → 删除仓库并用干净历史重建。

**教训(已写进 docs/ARCHITECTURE.md):** 豁免的**粒度**本身就是安全属性。
整文件白名单会豁免该文件未来新增的每一行 —— 它不是"例外",而是一个持续扩大的盲区。
`tests/test_privacy.py::TestPragmaGranularity` 现在锁住这个设计。

### 🔴 `.gitignore` 与实际配置格式不匹配

首版 `.gitignore` 写的是 `config/profile.yml`,而项目实际用 `.toml` ——
**真实配置整整一版没有被保护**。83 个测试里没有一个问过 git
"这个路径真的被忽略了吗"。

**处置:** 改用 `config/profile.*` 通配 + 新增 `tests/test_gitignore.py`,
直接调用 `git check-ignore` 验证**外部系统的实际行为**,而不是验证 `.gitignore` 的文本。

### 🔴 公开仓不得让 clone 者继承下单授权

v5.0.0 的 `AGENTS.md` 把"用户已长期授权自动下单"写成**项目事实**。
那对原作者是真的,但对任何 clone 者等于继承了一份别人的下单授权。

**处置:** 新增 `config/profile.toml` 的 `[execution]` 段,**仓库默认全部关闭**:
`enabled = false` / `dry_run = true` / `require_confirmation = true`,
外加单笔与单日金额上限、笔数上限、意图 TTL、报价时效、`kill_switch_file`。
`AGENTS.md` 与 `modes/trade.md` 改为**从配置读授权**,不再断言。
授权是本地事实,不是项目属性。

### 🟠 下单闸门从散文变成程序:`scripts/preflight.py`

v5.0.0 把"机械活下沉到脚本"写进架构原则,却把**最有后果的**几个机械检查
(尺寸占比、市场时段、意图时效、单日上限)留成了 `modes/trade.md` 里的散文。
这是本项目最大的内部矛盾。

新增 `preflight.py`,返回 `ALLOW` / `DRY_RUN` / `DENY`,覆盖:
紧急停止、`execution.enabled`、账户身份、市场时段与订单类型匹配、
**意图时效**、**报价时间戳熔断**、**减仓占比**(有专门的回归用例)、
残值仓、集中度、买力、现金底线、单日金额与笔数、同标的当日重复、
`ref_id` 格式与去重、理由标签合法性。46 个测试。

**散文约束在状态好的时候有效,程序约束一直有效。**

### 🟠 修正一条错误的「物理事实」:分数股交易限制

v5.0.0 把"分数股只能用市价单 + 正常时段,不支持限价、不支持盘前盘后"写成**绝对物理事实**,
并让多条纪律建立在它之上(盘前不下单、尾盘卖出有时间紧迫性)。

据券商官方文档,实际情况是:分数股**可以**在延长时段(盘前约 7:00–9:30、
盘后约 16:00–19:30 ET)用**限价单**交易,合格性按标的流动性逐一裁定;
24 小时市场则仅限整股。

**处置:** `_shared.md` §0/§1/§3/§5 全部改为**能力探测式**表述
(下单前用 `get_equity_tradability` 核实),并复核了所有依赖它的纪律 ——
尾盘卖出的紧迫性从"绝对"降为"程度问题",但结论(盘中最后窗口更可靠)不变。

**一条错误的"物理事实"会静默地为策略提供正当性,这比缺一个字段危险。**

### 🟡 其他

- CI `no-user-data` 现在扫描全部 git 历史并带自检(见上)。
- README 不再宣称"任何 Agent Skill CLI" —— 只有 Claude Code 有原生 skill 路由,
  其他 CLI 靠 `AGENTS.md` + 自然语言指定 mode。
- `doctor` 新增下单授权状态与紧急停止开关显示;FAQ 说明"全新 clone 未配置时非零退出是预期行为"。
- 测试 83 → 142。

### 已知未做

- **台账扩列(order_id / filled_at / 算术一致性校验 / 期初持仓 lot)** 尚未实施。
  影响:`ref_id` 去重只能尽力而为;已有的期初持仓会让 FIFO 报"卖出无对应买入"。
  `preflight.py` 与 `modes/stats.md` 都已显式声明这个限制,不假装完备。

---

## v5.0.0(2026-08-18)· 架构重构

从单体技能文件重构为可开源的分层系统。**纪律内容全部保留,只改组织方式。**

**新增**

- `AGENTS.md` 作为唯一共享上下文;`CLAUDE.md` 只做 `@AGENTS.md` 转发。
- 薄路由 `.claude/skills/meigu-ops/SKILL.md` + 8 个惰性加载的 `modes/*.md`。
- 系统层 / 用户层硬分离:`.gitignore` + `scripts/check_privacy.py` + CI `no-user-data`。
- 机器可读交易台账 `data/trades.tsv`(10 列 TSV,10 个受控 `reason_tag`)。
- 7 个 Python 脚本(零第三方依赖):交易日历、日报骨架、日志结构校验与压缩规划、
  FIFO 绩效统计、快照归档、环境自检、隐私检查。
- 83 个单元测试,重点覆盖交易日历边界和三个真实发生过的日志结构 bug。
- `modes/review.md` 的**规则有效性审计**机制:定期拿台账数据检验每条纪律,
  从未被数据支持过的规则要删掉——纪律手册第一次有了纠错闭环。
- `docs/`(SETUP / ARCHITECTURE / DATA_CONTRACT)、`examples/`(数据全虚构)、
  `DISCLAIMER.md`、MIT LICENSE。

**变更**

- 技能重命名 `meigu-trade` → `meigu-ops`,调用方式 `/meigu-ops <mode>`。
- 账户号从硬编码 7 处改为只存在于 gitignore 的 `config/profile.toml`。
- 单笔尺寸从写死的绝对美元改为 `config` 参数 `trade.size_std` / `trade.size_max`;
  仓位与现金规则一律用百分比。**副作用是好的:纪律变得与账户规模无关。**
- 关注股清单从模板硬编码改为 `config/watchlist.toml`。
- 两份内容重复的日报模板合并为 `templates/daily-report.md`(结构)
  + `modes/daily.md`(判断要点)。
- v1.0~v4.12 的更新日志从技能文件搬到本文件,不再占用每次会话的上下文。

**已废弃**

- 单体 `.claude/skills/meigu-trade/SKILL.md`(41KB,每次会话全量加载)。
- `auto_trade/` launchd 自动交易系统(2026-07-10 已停用,现归档至 `data/_archive/`)。

---

---

## 更早的版本

v1.0–v4.12 属于单体技能时期,其变更记录含具体交易细节,已移入用户层
(`data/history.md`,不进仓)。**策略演进不属于系统层版本日志** ——
今后规则变更记录在 `config/rules.toml` 各规则的 `evidence` 字段与 `data/lessons.md`。
