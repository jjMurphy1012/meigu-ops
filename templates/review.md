# 复盘 · {{PERIOD}}

> 生成于 {{GENERATED_AT}} · 数据来自 `make stats`
> 流程见 `modes/review.md`

## 一、数字(先看数据,再看叙事)

| 指标 | 本期 | 基准/目标 |
|------|------|----------|
| 组合表现 | | SMH / QQQ |
| 交易笔数 | | 笔数 <20 时比率无统计意义 |
| 实现盈亏(FIFO) | | |
| 胜率 | | |
| 平均持有天数 | | |
| BP 均值 | | 目标 < `cash.bp_target_pct` |
| 最大单一标的占比 | | 上限 `position.max_single_pct` |

## 二、标签绩效

| reason_tag | 笔数 | 实现盈亏 | 胜率 | 解读 |
|------------|------|---------|------|------|
| | | | | |

## 三、规则有效性审计

> 逐条来自 `config/rules.toml`(用 `make stats` 或 `make dashboard` 生成)。
> 本仓库不预设任何市场判断类规则 —— 这张表的行由你自己的规则决定。

| 规则 id | 陈述 | 判定 | 检验数据 | 建议(需你批准) |
|---------|------|------|---------|----------------|
| | | | | |

> **一条规则如果从未被数据支持过,它就是包袱不是资产。**

## 四、三类错误分类

**判断错**(依据充分、市场走反)—— 不改规则,这是交易的正常成本:
-

**纪律错**(规则说了但没执行)—— 不改规则,加检查点或脚本硬闸门:
-

**规则错**(照规则执行但结果系统性变差)—— 改规则,并写清为什么:
-

## 五、漏掉的机会(反向审计,防止越来越保守)

- 本期主线最强标的是否持有?没有的话是哪条规则挡住的?
- 零操作检查点里,多少是具体排查后的结论、多少是默认不动的惯性?
- 现金闲置的代价:BP 超出你设定目标 __/__ 天,期间标的池涨 __%,机会成本约 __
- 有无连续多日反复评估同一候选却始终没成交?

## 六、本期唯一要改的一件事

<!-- 只写一件。列十条改进 = 一条都不会改。 -->

## 七、手册更新

- [ ] 每条被审计的规则都追加了 `evidence` 并更新了 `last_audited`
- [ ] `status` 变更已获你本人批准后才写入 `config/rules.toml`
- [ ] `modes/_strategy.md` 的散文版已同步
- [ ] 被推翻的规则降为 `refuted`(**保留记录,不要直接删**)
- [ ] 策略变更**不进** `CHANGELOG.md` —— 那是系统层的版本日志
